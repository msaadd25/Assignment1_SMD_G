<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.I230077_assignment1">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginUserActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".SearchPageActivity" />
        <activity android:name=".DirectMessagesActivity" />
        <activity android:name=".ChatScreenActivity" />
        <activity android:name=".CallActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".Notification2Activity" />
        <activity android:name=".StoryViewActivity" />
        <activity android:name=".StoryCameraActivity" />
        <activity android:name=".StoryPublicationActivity" />
        <activity android:name=".StoryDetailsActivity" />
        <activity android:name=".StoryDetails2Activity" />
        <activity android:name=".PostActivity" />

    </application>

</manifest>